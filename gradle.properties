# Gradle Properties
org.gradle.jvmargs = -Xmx2G
org.gradle.parallel = true

# Mod Properties
mod.version = 2.2.1
mod.snapshot = false
maven_group = net.orifu
archives_base_name = skin_overrides

# dependencies
deps.quilt_loader = 0.27.0-beta.1

deps.minecraft = versioned
deps.quilt_mappings = versioned
deps.fabric_api = versioned
deps.modmenu = none
deps.ears = none

pub.should_publish = true
pub.additional_versions =
